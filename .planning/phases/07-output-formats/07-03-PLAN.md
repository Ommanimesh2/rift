---
phase: 07-output-formats
plan: 03
type: execute
depends_on: ["07-01", "07-02"]
files_modified: [cmd/root.go]
---

<objective>
Wire the --format flag in cmd/root.go to dispatch to FormatJSON or FormatMarkdown, replacing the current "not yet supported" fallback. Also add a --format=json integration smoke test.

Purpose: Phase 7's user-visible goal is the --format flag working end-to-end. The two formatters are built; this plan connects them to the CLI and validates the full pipeline.
Output: `imgdiff --format json` and `imgdiff --format markdown` produce correct output; unknown formats return a clear error; terminal (default) is unchanged.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-output-formats/07-01-SUMMARY.md
@.planning/phases/07-output-formats/07-02-SUMMARY.md
@cmd/root.go
@internal/output/format.go
@internal/output/json.go
@internal/output/markdown.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire --format flag to JSON and Markdown formatters</name>
  <files>cmd/root.go</files>
  <action>
    Replace the current format dispatch block in RunE (lines ~115-122) with a proper switch:

    switch flags.format {
    case "terminal", "":
        rendered := output.RenderTerminalWithSecurity(result, args[0], args[1], layerSummary, events)
        fmt.Print(rendered)
    case "json":
        data, err := output.FormatJSON(result, args[0], args[1], events)
        if err != nil {
            return fmt.Errorf("json formatting failed: %w", err)
        }
        fmt.Printf("%s\n", data)
    case "markdown":
        fmt.Print(output.FormatMarkdown(result, args[0], args[1], events))
    default:
        return fmt.Errorf("unknown format %q: supported formats are terminal, json, markdown", flags.format)
    }

    Remove the fmt.Fprintf(os.Stderr, "format %q not yet supported...") fallback line.
    Remove the `os` import if it is no longer used (check — os is still used for os.Stderr in the layer warning, so keep it).

    Also remove the debug "Opened ..." and "  Tree: ..." fmt.Printf lines from RunE — these were scaffolding output from Phase 2/3 that now pollutes the JSON/Markdown output. Replace with a single fmt.Fprintf(os.Stderr, ...) that only emits when format == "terminal" to preserve current terminal UX. Actually: just remove the Opened/Tree prints entirely — they were always placeholder debug output, not final UX.
  </action>
  <verify>
    go build ./... succeeds.
    go vet ./... clean.
    echo "format dispatch compiles" and manual inspection of switch cases.
  </verify>
  <done>
    cmd/root.go has the three-case switch, no "not yet supported" message, unknown format returns error.
    "Opened ..." debug lines removed from RunE.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add CLI integration tests for JSON and Markdown output</name>
  <files>cmd/root_test.go</files>
  <action>
    Create cmd/root_test.go (or add to it if it exists) with integration tests that exercise the format dispatch.

    Since real images aren't available in CI, test the format dispatch with the existing unit-test approach: build a DiffResult + SecurityEvents directly and call the output functions to confirm they're wired correctly.

    Write tests that verify:
    1. output.FormatJSON returns valid JSON with expected keys (use encoding/json to unmarshal and check Image1, Image2, summary fields)
    2. output.FormatMarkdown returns a string containing "## Image Diff" header and "### Summary" and "### Changes" sections

    These are lightweight integration checks confirming the formatters are importable and produce the right structure — not repeating the detailed behavior tests from 07-01/07-02.

    Test package: package cmd_test (external test package).
    Only test the output functions directly; do not try to invoke rootCmd.Execute() with fake images (requires Docker daemon or real registry).
  </action>
  <verify>
    go test ./cmd/... -v passes.
    go test ./... -v — full suite green.
  </verify>
  <done>
    cmd/root_test.go exists with at least 2 tests.
    All tests pass.
    No regressions in internal/output/... or internal/security/... tests.
  </done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `go build ./...` — compiles clean
- [ ] `go vet ./...` — no issues
- [ ] `go test ./...` — all tests pass (output, security, cmd packages)
- [ ] `cmd/root.go` has no "not yet supported" fallback
- [ ] Unknown format returns a clear error message (inspect the default case)
- [ ] Terminal format (default) behavior is unchanged
</verification>

<success_criteria>
- Both tasks completed
- All verification checks pass
- --format=json, --format=markdown, --format=terminal all handled
- Unknown formats return fmt.Errorf (not silent fallback)
- Phase 7 complete
</success_criteria>

<output>
After completion, create `.planning/phases/07-output-formats/07-03-SUMMARY.md`

Include in summary:
- What changed in cmd/root.go (format switch, debug line removal)
- Test coverage added
- Note: Phase 7 complete — JSON and Markdown output formats implemented and wired
</output>
