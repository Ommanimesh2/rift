---
phase: 08-performance-optimization
plan: 03
type: execute
depends_on: ["08-01", "08-02"]
files_modified: [cmd/root.go, internal/tree/tree_test.go]
domain: go
---

<objective>
Wire the two completed optimizations into the CLI:
1. Quick mode: add early return in RunE when flags.quick is true, using output.FormatQuick
2. Layer skip: call IdenticalLeadingLayers before building trees, use BuildFromImageSkipFirst
3. Streaming benchmark: add BenchmarkParseLayer to tree_test.go confirming memory O(file count)

Purpose: Both 08-01 and 08-02 built isolated functions. This plan connects them
to the user-visible CLI. After this plan, `--quick` actually produces output and
the full diff path automatically skips identical leading layers.

Output: cmd/root.go with both optimizations active. BenchmarkParseLayer in
tree_test.go confirming streaming behavior.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-performance-optimization/08-RESEARCH.md
@.planning/phases/08-performance-optimization/08-01-SUMMARY.md
@.planning/phases/08-performance-optimization/08-02-SUMMARY.md
@cmd/root.go
@internal/tree/tree.go
@internal/output/quick.go
@internal/output/layers.go
</context>

<tasks>

<task type="auto">
  <name>Wire --quick flag early return in cmd/root.go</name>
  <files>cmd/root.go</files>
  <action>
    In RunE, after both images are opened (after the img2 error check, before
    tree building), add an early return block for quick mode:

      if flags.quick {
          summary, err := output.CompareLayers(img1, img2)
          if err != nil {
              fmt.Fprintf(os.Stderr, "warning: layer comparison failed: %v\n", err)
          }
          fmt.Print(output.FormatQuick(summary, args[0], args[1], flags.format))
          return nil
      }

    Placement: immediately after img2 is successfully opened, before tree.BuildFromImage calls.
    CompareLayers uses only DiffID() and Size() — manifest-only, no downloads.

    Read cmd/root.go first to find the exact insertion point.
  </action>
  <verify>
    go build ./... succeeds.
    go vet ./... clean.
    go test ./cmd/... passes (existing integration tests unaffected).
    Code inspection: the quick block is before any tree.BuildFromImage calls.
  </verify>
  <done>
    cmd/root.go has the quick-mode early return.
    --quick with --format=json would produce JSON (verified by code inspection).
    Full diff path (flags.quick == false) is unchanged.
  </done>
</task>

<task type="auto">
  <name>Wire layer skip optimization into full diff path</name>
  <files>cmd/root.go</files>
  <action>
    After the quick-mode early return block (added in Task 1), replace the two
    tree.BuildFromImage calls with IdenticalLeadingLayers + BuildFromImageSkipFirst:

      // Compute identical leading layer count without downloading any content.
      skipCount, err := tree.IdenticalLeadingLayers(img1, img2)
      if err != nil {
          // Non-fatal: fall back to building full trees.
          skipCount = 0
      }

      tree1, err := tree.BuildFromImageSkipFirst(img1, skipCount)
      if err != nil {
          return fmt.Errorf("failed to build file tree for %q: %w", args[0], err)
      }

      tree2, err := tree.BuildFromImageSkipFirst(img2, skipCount)
      if err != nil {
          return fmt.Errorf("failed to build file tree for %q: %w", args[1], err)
      }

    Remove the old tree.BuildFromImage(img1) and tree.BuildFromImage(img2) calls.
    The rest of RunE (diff.Diff, output.CompareLayers, security.Analyze, format switch)
    is unchanged.

    Read the 08-01-SUMMARY.md to confirm the exported name is IdenticalLeadingLayers.
    If it was implemented as unexported (identicalLeadingLayers), update tree.go to
    export it before wiring.
  </action>
  <verify>
    go build ./... succeeds.
    go vet ./... clean.
    go test ./... — full suite green.
    Code inspection: no calls to tree.BuildFromImage remain in cmd/root.go.
  </verify>
  <done>
    cmd/root.go calls tree.IdenticalLeadingLayers then tree.BuildFromImageSkipFirst for both images.
    skipCount=0 (error or no shared layers) falls back to identical behavior as before.
    No regressions in existing integration tests.
  </done>
</task>

<task type="auto">
  <name>Add streaming benchmark to tree_test.go</name>
  <files>internal/tree/tree_test.go</files>
  <action>
    Add BenchmarkParseLayer_Streaming to tree_test.go:

    // BenchmarkParseLayer_Streaming confirms ParseLayer processes layer content
    // in streaming fashion. Memory usage is O(file count × metadata) not O(layer size).
    // Run with: go test -bench=. -benchmem ./internal/tree/...
    //
    // To verify streaming: increase perFileBytes to 10000 — alloc count should
    // stay the same while layer size grows 10x.
    func BenchmarkParseLayer_Streaming(b *testing.B) {
        const fileCount = 1000
        const perFileBytes = 1024

        layer := makeFakeLayer(b, fileCount, perFileBytes)
        b.ResetTimer()
        b.ReportAllocs()

        for i := 0; i < b.N; i++ {
            _, err := ParseLayer(layer)
            if err != nil {
                b.Fatal(err)
            }
        }
    }

    The makeFakeLayer helper needs to be added or verified to exist in tree_test.go.
    Look at existing test helpers in tree_test.go:
    - If buildTar / fakeLayer helpers exist: use them to build makeFakeLayer(b testing.TB, fileCount, perFileBytes int) v1.Layer
    - If not: create a simple helper that builds a tar with fileCount files of perFileBytes each using a bytes.Buffer

    Keep the benchmark under 30 lines. Reuse existing test infrastructure.
  </action>
  <verify>
    go test -bench=BenchmarkParseLayer -benchmem ./internal/tree/... runs without error.
    go test ./internal/tree/... — all non-benchmark tests still pass.
    go build ./... clean.
  </verify>
  <done>
    BenchmarkParseLayer_Streaming exists in tree_test.go.
    Benchmark runs with go test -bench=. -benchmem ./internal/tree/...
    Comment documents the streaming guarantee and how to verify it.
  </done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `go build ./...` — compiles clean
- [ ] `go vet ./...` — no issues
- [ ] `go test ./...` — all tests pass (tree, output, diff, security, cmd packages)
- [ ] `go test -bench=. -benchmem ./internal/tree/...` — benchmark runs
- [ ] cmd/root.go: --quick early return present and before tree building
- [ ] cmd/root.go: tree.BuildFromImageSkipFirst used for both trees
- [ ] cmd/root.go: tree.IdenticalLeadingLayers called before tree builds
- [ ] No regressions in any existing test
</verification>

<success_criteria>
- All three tasks completed
- All verification checks pass
- --quick flag now produces real output (not silent/ignored)
- Full diff path skips identical leading layers automatically
- Streaming documented with a benchmark
- Phase 8 complete
</success_criteria>

<output>
After completion, create `.planning/phases/08-performance-optimization/08-03-SUMMARY.md`

Include in summary:
- Changes to cmd/root.go (quick early return, layer skip wiring)
- Export status of IdenticalLeadingLayers (renamed or confirmed)
- Benchmark added and example output
- Phase 8 complete declaration
</output>
