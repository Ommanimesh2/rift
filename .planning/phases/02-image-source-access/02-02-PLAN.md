---
phase: 02-image-source-access
plan: 02
type: execute
depends_on: ["02-01"]
files_modified: [cmd/root.go]
---

<objective>
Wire the image source package into the CLI so imgdiff actually opens both images when invoked.

Purpose: Connect Phase 1's CLI skeleton to Phase 2's source abstraction — after this plan, running `imgdiff nginx:1.24 nginx:1.25` actually pulls and opens both images.
Output: Updated root command that resolves both image references through source.Open() and reports image metadata.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-image-source-access/02-RESEARCH.md
@.planning/phases/02-image-source-access/02-01-SUMMARY.md
@cmd/root.go

**Tech stack available:** cobra v1.10.2, go-containerregistry v0.21.2 (from Plan 01)
**Established patterns:** cobra persistent flags on root, flags struct in cmd/root.go
**Constraining decisions:**
- Phase 1: --platform flag already defined as string
- Phase 2 Plan 01: source.Open(ref, opts) returns v1.Image
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire source.Open() into root command RunE</name>
  <files>cmd/root.go</files>
  <action>
  Update the root command's RunE function in cmd/root.go:

  1. Import the source package: "github.com/ommmishra/imgdiff/internal/source"
  2. Replace the placeholder fmt.Printf with actual image opening:
     - Create source.Options with Platform from flags.platform
     - Call source.Open(args[0], opts) for image 1
     - Call source.Open(args[1], opts) for image 2
     - On error: return fmt.Errorf("failed to open image %q: %w", ref, err) — cobra will display it
  3. After both images opened successfully, print confirmation:
     - fmt.Printf("Opened %s (%s)\n", args[0], source.DetectSourceType(args[0]))
     - fmt.Printf("Opened %s (%s)\n", args[1], source.DetectSourceType(args[1]))
     - Print layer count from each image: img.Layers() returns ([]v1.Layer, error)
     - Print manifest digest from each image: img.Digest() returns (v1.Hash, error)
  4. Return nil (future phases will pass these images to the diff engine)

  Keep it simple — this is a stepping stone. The actual diff output comes in Phase 4-5.
  Do NOT store images globally — they'll be passed as arguments to diff functions in later phases.
  Error messages should clearly indicate which image failed and what source type was attempted.
  </action>
  <verify>go build -o imgdiff . succeeds, ./imgdiff --help still shows correct usage</verify>
  <done>Root command compiles with source.Open() calls, error handling covers both images, build produces working binary</done>
</task>

<task type="auto">
  <name>Task 2: Verify end-to-end with build and --help</name>
  <files>cmd/root.go</files>
  <action>
  Final verification pass:

  1. Run `go build -o imgdiff .` — must succeed
  2. Run `go vet ./...` — must be clean
  3. Run `go test ./...` — all tests must pass (including source detection tests from Plan 01)
  4. Run `./imgdiff --help` — must show updated usage with image source types mentioned
  5. Run `./imgdiff version` — must still work (regression check)
  6. Run `./imgdiff nonexistent:image1 nonexistent:image2` — should produce clear error about image access failure (not a panic)

  If any issues found, fix them. This is the final quality gate for Phase 2.

  Clean up the built binary after verification: rm -f imgdiff
  </action>
  <verify>go build ./... clean, go test ./... passes, go vet ./... clean, binary runs without panics</verify>
  <done>Binary builds, all tests pass, --help works, version subcommand works, error cases produce readable messages (no panics)</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go build -o imgdiff .` succeeds
- [ ] `go vet ./...` clean
- [ ] `go test ./...` all pass
- [ ] `./imgdiff --help` shows correct usage
- [ ] `./imgdiff version` works (regression check)
- [ ] Error messages are clear when images can't be opened
- [ ] Phase 2 complete — image source access fully wired
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Root command calls source.Open() for both image arguments
- Platform flag is passed through to source options
- Error handling is clear and doesn't panic
- All prior functionality preserved (--help, version)
- Phase 2: Image Source Access complete
</success_criteria>

<output>
After completion, create `.planning/phases/02-image-source-access/02-02-SUMMARY.md`
</output>
