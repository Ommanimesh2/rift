---
phase: 04-diff-engine
plan: 02
type: execute
depends_on: ["04-01"]
files_modified: [cmd/root.go]
---

<objective>
Wire the Diff engine into the root CLI command so running imgdiff compares two images and prints the diff summary.

Purpose: Connect the diff algorithm to the existing CLI pipeline — the first time imgdiff actually compares images.
Output: Running `imgdiff <image1> <image2>` builds trees, computes diff, and prints a summary line.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-diff-engine/04-01-SUMMARY.md
@cmd/root.go
@internal/diff/diff.go

**Tech stack available:** cobra, go-containerregistry, internal/tree, internal/diff
**Established patterns:** CLI prints "Opened REF (source-type)" then "  Tree: ..." per image
**Constraining decisions:**
- DiffResult.String() provides human-readable summary line
- Phase 5 handles detailed per-file output — this task only adds summary line
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Diff into root command and print summary</name>
  <files>cmd/root.go</files>
  <action>
    Import the internal/diff package. After building both file trees (tree1, tree2), call diff.Diff(tree1, tree2) to compute the diff result. Print the diff summary after both tree summaries.

    Output format:
    ```
    Opened nginx:1.24 (remote)
      Tree: 152 files, 23 directories, 45.2 MB total
    Opened nginx:1.25 (remote)
      Tree: 155 files, 23 directories, 46.1 MB total

    Diff: 5 added, 2 removed, 3 modified (+1.2 MB / -200 bytes)
    ```

    Implementation:
    1. Add import for "github.com/ommmishra/imgdiff/internal/diff"
    2. After tree2 is built (after the second fmt.Printf Tree line), add:
       - fmt.Println() for blank separator line
       - result := diff.Diff(tree1, tree2)
       - fmt.Printf("Diff: %s\n", result)
    3. Keep existing "Opened" and "Tree" lines unchanged

    Do NOT add per-file listing, color output, or detailed breakdown — Phase 5 handles that.
  </action>
  <verify>go build ./... compiles, go vet ./... clean, go test ./... passes</verify>
  <done>Root command imports diff package, calls Diff(tree1, tree2), prints "Diff: ..." summary line after tree summaries</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] go build ./... succeeds
- [ ] go vet ./... reports no issues
- [ ] go test ./... passes all tests (tree + diff packages)
- [ ] Binary compiles and runs without error
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Root command prints diff summary after tree summaries
- No errors or warnings introduced
- Phase 4 complete
</success_criteria>

<output>
After completion, create `.planning/phases/04-diff-engine/04-02-SUMMARY.md`
</output>
